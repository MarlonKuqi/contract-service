# Port aléatoire pour éviter les conflits lors de l'exécution parallèle des tests
server:
  port: 0

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    com.mk.contractservice: DEBUG


